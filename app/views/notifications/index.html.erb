<div class="container py-4">
  <div class="p-5 mb-4 rounded-3">
    <div class="container-fluid py-5">
      <h3 class="display-5">通知</h1>
      <% if @notifications.exists? %>
        <p class="col-md-8 fs-4"><%= render @notifications %></p>
      <% else %>
        <div class="border border-3 rounded-3 p-3">
          <p>通知はありません</p>
        </div>
      <% end %>
    </div>
  </div>
</div>
<%=link_to 'Back', 'javascript:history.back()', class:"btn btn-purple mx-auto d-block w-25" %>